events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    client_max_body_size 0;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    server {
        listen 80;
        server_name _;
        return 301 https://$host$request_uri;
    }

    # --- GITEA ---
    server {
        listen 443 ssl;
        server_name gitea.local;

        ssl_certificate     /etc/nginx/certs/gitea.local.pem;
        ssl_certificate_key /etc/nginx/certs/gitea.local-key.pem;

        location / {
            proxy_pass http://gitea:3000;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    # --- PGADMIN ---
    # server {
    #     listen 443 ssl;
    #     server_name pgadmin.local;

    #     ssl_certificate     /etc/nginx/certs/pgadmin.local.pem;
    #     ssl_certificate_key /etc/nginx/certs/pgadmin.local-key.pem;

    #     location / {
    #         proxy_pass http://pgadmin:80;

    #         proxy_set_header X-Script-Name "";
    #     }
    # }

    # --- DRONE SERVER ---
    # server {
    #     listen 443 ssl;
    #     server_name drone-server.local;

    #     ssl_certificate     /etc/nginx/certs/drone-server.local.pem;
    #     ssl_certificate_key /etc/nginx/certs/drone-server.local-key.pem;

    #     location / {
    #         proxy_pass http://drone-server:80;

    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";

    #         chunked_transfer_encoding off;
    #     }
    # }
}
